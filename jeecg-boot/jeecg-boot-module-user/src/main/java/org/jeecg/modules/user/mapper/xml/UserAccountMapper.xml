<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.user.mapper.UserAccountMapper">

    <!-- 获取最大UserId -->
    <select id="getUserMaxId" resultType="org.jeecg.modules.user.entity.UserAccount">
		select max(user_id) from  user_account
	</select>

</mapper>